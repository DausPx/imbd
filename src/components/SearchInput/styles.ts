import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  inputWrapper: {width: '100%', height: 70},
  input: {height: 40, margin: 12, borderWidth: 1, padding: 10},
});

export default styles;
